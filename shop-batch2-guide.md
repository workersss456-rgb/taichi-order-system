# 商城端調整說明（第二批）

## ⚠️ 檔案覆蓋順序提醒
這批的 `public/js/admin.js` 已經包含**第一批（管理後台加寬/篩選/防重複）的全部內容**，是最新的完整版本。你只要用這批的 `admin.js` 覆蓋就好，不用再回去用第一批那份，兩份疊著用反而會互相蓋掉。

## 這次改了什麼

### 1. 手機版一列 2 欄
商城品項卡片在手機（寬度 860px 以下）改成固定一列 2 欄。因為空間有限，手機版把商品說明文字先隱藏、圖片和按鈕都縮小了一點，核心的圖片／名稱／規格／顏色／數量／加入購物車都還在。

### 2. 歷史紀錄摺疊顯示
現在每筆訂單預設只顯示一行摘要（單號、狀態、訂購人、案場、共幾項/幾件、時間），點一下整行就會展開看到完整的品項清單、備註、操作按鈕。再點一次收合。

### 3. 歷史紀錄狀態頁籤
上方新增 5 個頁籤：**送出訂單 / 採購處理中 / 廠商處理中 / 現場回報異常 / 已結案**，每個頁籤旁邊顯示筆數，點了只看那個狀態的訂單。頁籤數字是根據你搜尋條件（姓名/日期）篩出來的結果去算的，換頁籤不用重新搜尋。

流程維持你原本確認過的版本，**異常是獨立的狀態**，不會跟「已結案」混在一起：
```
送出訂單 → 採購處理中 → 廠商處理中 ┬→ 現場收貨無異常 → 已結案
                                    └→ 現場收貨有異常 → 現場回報異常 → 採購處理完成 → 已結案
```

### 4. 現場收貨可勾選異常項目
按「📦 現場收貨確認」→ 選「有異常」時，會列出這張單的所有品項讓你勾選是哪幾項有問題，下面填**一個**整體的問題描述（不是每項都要分別寫）。

勾選的項目在**後台**（管理後台的歷史紀錄）會用 ⚠️ 紅字標示出來，採購一眼就知道要處理哪幾項，其他正常的項目不用管。商城端自己的歷史紀錄裡有異常的品項也會一樣標示。

如果現場沒有特別勾任何項目、只填了問題描述，系統會當作整張單都有狀況處理。

## 這次變動的檔案
```
db.js                       修改（order_items 新增 has_issue 欄位）
routes/publicApi.js         修改（現場收貨 API 支援勾選的品項清單）
public/index.html           修改（收貨確認視窗加上品項勾選、歷史紀錄加頁籤容器）
public/js/shop.js           修改（狀態頁籤、摺疊卡片、收貨勾選邏輯）
public/js/admin.js          修改（異常品項標紅）—— 已包含第一批全部內容
public/css/style.css        修改（手機2欄、頁籤樣式、摺疊樣式）
```
`server.js`、`package.json`、`mailer.js`、`config.js`、`routes/adminApi.js`、`middleware/adminAuth.js`、`public/admin.html`、`public/js/api.js`、`public/js/print-order.js`、`public/print-order.html`、`public/images/logo.png` 這次沒有變動。

## 部署提醒
`order_items.has_issue` 欄位一樣是自動遷移（`ADD COLUMN IF NOT EXISTS`），現有資料不受影響，新欄位預設是「沒有異常」。

## 建議測試
1. 用手機（或瀏覽器縮小視窗到手機寬度）看商城，確認 2 欄排版看得清楚、能正常加入購物車
2. 歷史紀錄點頁籤切換，確認筆數對得上、點卡片能展開收合
3. 對一筆「廠商處理中」的訂單按現場收貨確認 → 選有異常 → 勾 1-2 項 → 填描述送出
4. 回後台歷史紀錄找那筆單，確認勾選的品項有標紅、狀態變成「現場回報異常」
