# 階段三更新說明：訂單狀態流程 + 廠商報價

## 一、訂單狀態流程

五個狀態：**送出訂單 → 採購處理中 → 廠商處理中 → 已結案**，中間若現場收貨有問題，會岔出 **現場回報異常** 再回到結案。

### 誰在哪裡操作

**管理後台（叫料歷史紀錄）** — 每筆訂單下方會依目前狀態出現對應的按鈕：
- 狀態「送出訂單」→ 按「→ 採購處理中」
- 狀態「採購處理中」→ 按「→ 廠商處理中」
- 狀態「廠商處理中」→ 按「✓ 直接結案」（如果不需要現場確認）
- 狀態「現場回報異常」→ 填寫「採購處理內容」存檔後，按「✓ 處理完成，結案」

上方篩選列新增了「訂單狀態」下拉，可以只看待處理的單。

**商城歷史紀錄（一般使用者／現場）** — 未結案的訂單會有「📦 現場收貨確認」按鈕，點開後選：
- **無異常** → 直接結案
- **有異常** → 填寫問題描述 → 狀態變成「現場回報異常」，拋轉回採購

### Email 通知
「現場回報異常」和「結案」這兩個時間點會寄信，收件人是**管理員 + 訂購人本人**（訂購人有留 Email 才會寄）。中間的採購/廠商處理階段不寄信，避免信箱被灌爆。

### ⚠️ 照片上傳這次沒做
依你的決定先取消，避免撞到容量上限。現場回報異常目前是純文字描述。之後如果要加，可以再討論用外部圖床的做法。

---

## 二、廠商報價 / 材料預估

管理後台每筆訂單下方有一個可展開的「💰 廠商報價 / 材料預估」區塊（預設收合，不會擋住畫面）：

- 每個品項一列，填**牌價**和**折數**
- **單價 = 牌價 × 折數**，邊打邊自動算出來，小計和總計也即時更新
- 折數請填**小數**：75 折填 `0.75`、9 折填 `0.9`（輸入框和下方說明都有標示）
- 填完按「儲存報價」。單價是由後端重新計算後才存的，不會有前端算一套、資料庫存另一套的問題

CSV 匯出也一併加上了狀態、牌價、折數、單價、小計、異常說明、採購處理內容等欄位，可以直接丟進 Excel 做成本分析。

---

## 三、兩種列印單據

現在列印按鈕分成兩個（在管理後台的歷史紀錄）：

| 按鈕 | 內容 | 用途 |
|---|---|---|
| 🖨️ 廠商訂購單（不含價格） | 項次/名稱/規格/數量/單位/備註 | 傳給廠商訂貨，看不到你的成本 |
| 🖨️ 內部核簽單（含價格） | 上述欄位 + 牌價/折數/單價/小計 + 合計 | 內部簽核用，標題會多註明「（內部核簽）」 |

商城端（送出成功畫面、一般使用者的歷史紀錄）的列印按鈕**一律是不含價格的廠商版**，避免成本資訊外流給沒有後台權限的人。

內部核簽單因為欄位多，字級會自動縮小一點避免擠爆版面。

---

## 這次變動的檔案
```
db.js                       修改（orders 狀態欄位、order_items 報價欄位）
mailer.js                   修改（新增訂單狀態通知信）
routes/publicApi.js         修改（現場收貨確認 API）
routes/adminApi.js          修改（狀態推進、報價、CSV 欄位）
public/index.html           修改（現場收貨確認視窗）
public/admin.html           修改（訂單狀態篩選）
public/js/shop.js           修改（狀態顯示、收貨確認）
public/js/admin.js          修改（狀態按鈕、報價表格、雙列印按鈕）
public/print-order.html     修改（價格欄位樣式）
public/js/print-order.js    修改（含價格/不含價格兩種模式）
```

`server.js`、`package.json`、`config.js`、`middleware/adminAuth.js`、`public/css/style.css`、`public/js/api.js`、`public/images/logo.png` 這次沒有變動。

## 部署注意
- 沒有新增 npm 套件，不用動 package-lock.json
- 資料庫欄位會在重新部署時自動新增（`ADD COLUMN IF NOT EXISTS`），現有訂單資料不會受影響
- **現有的舊訂單狀態會是空的**，畫面上會當成「送出訂單」顯示。如果想讓舊訂單直接變成已結案，跟我說，我給你一句 SQL 到 Neon 後台執行就好

## 建議測試順序
1. 下一筆新訂單 → 確認狀態顯示「送出訂單」
2. 後台按「→ 採購處理中」、填廠商、填幾筆報價存檔 → 確認單價、小計、總計算得對
3. 印一次「廠商訂購單」、一次「內部核簽單」→ 確認價格有/沒有正確顯示
4. 商城歷史紀錄按「現場收貨確認」→ 選「有異常」填描述 → 確認狀態變成異常、信有寄出
5. 後台填採購處理內容 → 按結案 → 確認狀態變已結案、信有寄出
